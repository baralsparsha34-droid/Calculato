name: Keep Web Service Awake_Calculator
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request
        run: curl -I https://calculator-5o4v.onrender.com/
